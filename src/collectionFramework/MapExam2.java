package collectionFramework;

import java.util.*;

public class MapExam2 {
	public Map<String, Integer> makeMap() {
		Map<String, Integer> products = new HashMap<>();
		products.put("가위", 2500);
		products.put("크래파스", 5000);
		// 상품의 이름과 값을 products에 추가해 보세요.

		return products;
	}

	public static void main(String[] args) {
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		/*MapExam2 ME = new MapExam2();
		// 키값을 구함.
		Set<String> keys = ME.makeMap().keySet();
		Iterator<String> iter = keys.iterator();
		while (iter.hasNext()) {
			String key = iter.next();
			int value = ME.makeMap().get(key);
			System.out.println(key + ":" + value);
		}
*/
	}
}